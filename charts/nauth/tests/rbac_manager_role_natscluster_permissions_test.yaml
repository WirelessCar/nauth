suite: manager role permissions
templates:
  - templates/rbac_manager_role.yaml
tests:
  - it: grants update on natsclusters for finalizer management
    asserts:
      - contains:
          path: rules
          content:
            apiGroups:
              - nauth.io
            resources:
              - natsclusters
            verbs:
              - get
              - list
              - update
              - watch

  - it: grants update on natsclusters finalizers
    asserts:
      - contains:
          path: rules
          content:
            apiGroups:
              - nauth.io
            resources:
              - accounts/finalizers
              - natsclusters/finalizers
              - users/finalizers
            verbs:
              - update

  - it: grants write access to events.k8s.io events
    asserts:
      - contains:
          path: rules
          content:
            apiGroups:
              - events.k8s.io
            resources:
              - events
            verbs:
              - create
              - patch
