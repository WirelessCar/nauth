apiVersion: nauth.io/v1alpha1
kind: AccountImport
metadata:
  name: import-rule
spec:
  accountName: import-account
  exportAccountRef:
    name: export-account
    namespace: {{ .Namespace }}
  rules:
    - name: not-allowed-rule
      subject: not-allowed.>
      type: stream
