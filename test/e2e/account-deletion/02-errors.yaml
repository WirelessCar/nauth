# This file contains resources that will cause test to fail if they exist.
# The test ensures that no Account or Secret resources are left behind after
# an account deletion operation.

apiVersion: nauth.io/v1alpha1
kind: Account
metadata:
  name: example-account

---
apiVersion: v1
kind: Secret
metadata:
  labels:
    nauth.io/secret-type: account-root

---
apiVersion: v1
kind: Secret
metadata:
  labels:
    nauth.io/secret-type: account-sign
