apiVersion: nauth.io/v1alpha1
kind: AccountExport
metadata:
  name: my-account-export-a
spec:
  accountName: my-account
  rules:
    - name: conflicting-a
      subject: conflict.>
      type: stream

