apiVersion: nauth.io/v1alpha1
kind: Account
metadata:
  name: import-account
spec:
  natsClusterRef:
    namespace: nats
    name: local-nats
---
apiVersion: nauth.io/v1alpha1
kind: Account
metadata:
  name: export-account
  namespace: {{ .Namespace }}-export
spec:
  natsClusterRef:
    namespace: nats
    name: local-nats
