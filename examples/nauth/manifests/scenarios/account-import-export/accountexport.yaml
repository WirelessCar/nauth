# AccountExport example manifest
# NOTE: this is experimental, work in progress, protected by feature flag

apiVersion: nauth.io/v1alpha1
kind: AccountExport
metadata:
  name: my-account-export
  namespace: account-ie-test
spec:
  accountName: my-account
  rules:
    - name: test-rule
      subject: my-subject.>
      type: stream
